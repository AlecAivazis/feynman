// local imports
import { sidebarWidth } from '../Sidebar/styles'
// import { toolbarWidth } from '../Toolbar/styles'

export default {
    container: {
        position: 'absolute',
        top: 20,
        left: sidebarWidth,
        right: 170,
        textAlign: 'center',
    }
}