// local imports
import { sidebarWidth } from '../Sidebar/styles'
import { toolbarWidth } from '../Toolbar/styles'

export default {
    container: {
        position: 'absolute',
        top: 20,
        left: sidebarWidth,
        right: toolbarWidth,
        textAlign: 'center',
        fontSize: 25,
        fontWeight: 400,
    }
}
