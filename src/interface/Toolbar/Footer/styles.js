import { brightBlue } from 'colors'

export default {
    container: {
        color: 'white',
        fontSize: 14,
    },
    line: {
        textAlign: 'center',
        marginBottom: 15,
    },
    link: {
        color: brightBlue,
        marginLeft: 5,
    }
}
