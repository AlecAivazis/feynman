// local imports
import { red } from 'colors'

export default { 
    container: {

    },
    buttonRow: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
    },
    closeButton: {
        marginRight: 8,
        color: red,
    }
}