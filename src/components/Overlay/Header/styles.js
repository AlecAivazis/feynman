// local imports
import { otherBlue } from 'colors'

export default {
    container: {
        backgroundColor: otherBlue,
        padding: '2%',
        borderRadius: '6px 6px 0 0',
        alignItems: 'center',
        justifyContent: 'space-between',
        display: 'flex',
        flexDirection: 'row',
        color: 'white',
        fontSize: 18,
    },
    title: {
        fontWeight: 'bold',
    },
    close: {
        cursor: 'pointer',
        textTransform: 'capitalize',
        fontWeight: 'bold',
    }
}