import { otherRed } from 'colors'

export default {
    container: {
        background: "#c1403e linear-gradient(180deg, #cd6664, #c1403e) repeat-x",
        color: 'white',
        border: `1px solid ${otherRed}`,
        textShadow: '1px 1px 1px rgba(0,0,0,0.1)',
    }
}
