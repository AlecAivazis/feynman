import { elementSpacing } from '../styles'

export default {
    container: {
        display: 'flex',
        flexDirection: 'column',
    },
    label: {
        marginBottom: elementSpacing,
    },
    input: {
        marginBottom: elementSpacing,
    },
}